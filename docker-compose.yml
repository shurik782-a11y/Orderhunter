services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: orderhunter
      POSTGRES_PASSWORD: orderhunter
      POSTGRES_DB: orderhunter
    ports:
      - "5433:5432"
    volumes:
      - orderhunter_pg:/var/lib/postgresql/data

volumes:
  orderhunter_pg:
